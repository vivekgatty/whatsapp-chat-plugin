export const runtime="nodejs"; export const dynamic="force-dynamic"; export const revalidate=0;
export default function Pricing(){
  return <main className="p-6">Pricing / subscription management — placeholder.</main>;
}
