"use client";

/**
 * Browser-side Supabase client for use in Client Components.
 * Re-exports from the existing browser module for consistency.
 */
export { getBrowserSupabase, getBrowserSupabase as createBrowserClient } from "./browser";
